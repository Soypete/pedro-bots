apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "reddit-watcher.fullname" . }}-cfp
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "reddit-watcher.labels" . | nindent 4 }}
    app: cfp
spec:
  schedule: {{ .Values.cfp.schedule | quote }}
  concurrencyPolicy: {{ .Values.cfp.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.cfp.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.cfp.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      activeDeadlineSeconds: {{ .Values.cfp.activeDeadlineSeconds }}
      template:
        metadata:
          labels:
            {{- include "reddit-watcher.selectorLabels" . | nindent 12 }}
            app: cfp
          {{- if .Values.vault.enabled }}
          annotations:
            {{- include "reddit-watcher.vaultAnnotations" . | nindent 12 }}
          {{- end }}
        spec:
          restartPolicy: OnFailure
          containers:
            - name: {{ .Chart.Name }}
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              {{- include "reddit-watcher.agentArgs" (dict "agent" "cfp" "root" .) | nindent 14 }}
              envFrom:
                {{- include "reddit-watcher.envFrom" . | nindent 16 }}
              {{- with .Values.resources }}
              resources:
                {{- toYaml . | nindent 16 }}
              {{- end }}
