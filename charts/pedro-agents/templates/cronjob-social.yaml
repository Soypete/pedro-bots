apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "reddit-watcher.fullname" . }}-social
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "reddit-watcher.labels" . | nindent 4 }}
    app: social
spec:
  schedule: {{ .Values.social.schedule | quote }}
  concurrencyPolicy: {{ .Values.social.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.social.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.social.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      activeDeadlineSeconds: {{ .Values.social.activeDeadlineSeconds }}
      template:
        metadata:
          labels:
            {{- include "reddit-watcher.selectorLabels" . | nindent 12 }}
            app: social
          {{- if .Values.vault.enabled }}
          annotations:
            {{- include "reddit-watcher.vaultAnnotations" . | nindent 12 }}
          {{- end }}
        spec:
          restartPolicy: OnFailure
          containers:
            - name: {{ .Chart.Name }}
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              {{- include "reddit-watcher.agentArgs" (dict "agent" "social-poster" "root" .) | nindent 14 }}
              envFrom:
                {{- include "reddit-watcher.envFrom" . | nindent 16 }}
              {{- with .Values.resources }}
              resources:
                {{- toYaml . | nindent 16 }}
              {{- end }}
